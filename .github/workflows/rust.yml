name: Rust

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    runs-on: ubuntu-latest

    services:
      # Label used to access the service container
      postgres:
        # Docker Hub image
        image: postgres:10.8
        # Provide the password for postgres
        env:
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: newsletter
        ports:
          - 5432:5432
        # Set health checks to wait until postgres has started
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - uses: actions/checkout@v3
      - name: Install sqlx-cli
        run: cargo install sqlx-cli

      # use var in .env file
      #- name: Set
      #  run: export DATABASE_URL=postgres://postgres:postgres@127.0.0.1:5432/newsletter

      - name: Mig
        run: sqlx migrate run

      - name: Build
        run: cargo build

      - name: Run tests
        run: cargo test
